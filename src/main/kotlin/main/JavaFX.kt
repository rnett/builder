package main

/*
import com.rnett.kframe.hosts.javafx.javaFXKFrame
import javafx.application.Application
import javafx.scene.Scene
import javafx.stage.Stage
import org.jetbrains.exposed.sql.Database
import pages.materialsPage
import tests.myPage
//TODO doesn't work, unsure why
class JFxTest : Application() {
    override fun start(stage: Stage?) {
        connect()


        stage!!

        val kframe = javaFXKFrame("") {
            page("", builder = materialsPage)
        }

        stage.title = "KFrame JavaFX Test"
        stage.scene = Scene(kframe.webView)

        stage.show()

    }

    companion object {
        fun main(args: Array<String>) {
            connect()
            launch(*args)
        }
    }

}

        */
